<%= form_with(model: permissao) do |form| %>
  <% if permissao.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(permissao.errors.count, "error") %> prohibited this permissao from being saved:</h2>

      <ul>
        <% permissao.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :usuario_id, style: "display: block" %>
    <%= form.text_field :usuario_id %>
  </div>

  <div>
    <%= form.label :papel, style: "display: block" %>
    <%= form.text_field :papel %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
