<%= form_with(model: inscricao) do |form| %>
  <% if inscricao.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inscricao.errors.count, "error") %> prohibited this inscricao from being saved:</h2>

      <ul>
        <% inscricao.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :participante, style: "display: block" %>
    <%= form.text_field :participante %>
  </div>

  <div>
    <%= form.label :torneio_id, style: "display: block" %>
    <%= form.text_field :torneio_id %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
